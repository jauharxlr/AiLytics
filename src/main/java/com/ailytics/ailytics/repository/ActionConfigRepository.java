package com.ailytics.ailytics.repository;

import com.ailytics.ailytics.model.ActionConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ActionConfigRepository extends JpaRepository<ActionConfig, String> {
}
